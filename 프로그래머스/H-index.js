/*
foreach 반복문은 오직 Array 객체에서만 사용가능한 메서드입니다. 
배열의 요소들을 반복하여 작업을 수행할 수 있고 foreach 구문의 인자로 callback 함수를 등
  var items = ['item1', 'item2', 'item3'];

      items.forEach(function(item) {

   console.log(item);

   });

   // 출력 결과: item, item2, item3





for in() 
 배열이나 객체를 더욱 쉽게 다룰 수 있도록 for in 반복문을

var array = ['포도', '사과', '바나나', '망고'];
   for( var i in array ) {

     alert(array[i]);

   }
*/



function solution(arr) {
     for(let array in arr ){
         if(array>arr.len)
     }
}