function solution(numbers) {
    let front_num;
    for (let number of numbers)
    if(number>10)
        let num=front_num=number/10
    else if (number>100)
        let num=front_num=number/100
        
    
    
    

             
    return answer;
}