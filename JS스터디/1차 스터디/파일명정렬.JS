// 정규표현식 공부하기 -  https://hamait.tistory.com/342
// sort함수 공부하기 

// 1. HEAD를 기준으로 대소문자 구분 없이 사전 순으로 정렬
// 2. HEAD가 같다면, NUMBER를 기준으로 숫자 순으로 정렬  
// 3. 까지 동일하다면 입력 순으로 정렬

// HEAD : 파일명에서 숫자가 나오기 전까지의 문자들
// NUMBER : HEAD 다음의 숫자만으로 구성된 부분
// TAIL : 나머지 부분




// **************************************************
function solution(files) {
  return files.sort((a, b) => {
 
     여기다 정렬을 정의해서 박는다 
     -전 정규표현식을 사용해서 했습니다

     *정규표현식 : 검색 패턴을 형성하는 문자열을 의미 -> 정해논 패턴규칙을 내가 조합해서 조건을 더해주는식

  });
}
// **************************************************




1. 헤더 구하기 (헤더 : 파일명에서 숫자가 나오기 전까지의 문자들)
let 헤더 = a.match(/^\D+/)[0].toLowerCase();
///^\D+/  : 시작부터 숫자가 나오기 전까지의 문자열
//  ^ :  입력의 시작 부분에 대응
// \D :숫자가 아닌 문자에 대응
//  + :앞의 표현식이 1회 이상  - 연속으로 반복되는 부분에 대응
//toLowerCase : 소문자변형

2. 넘버 구하기 (넘버 : 헤더다음에 나오는 연속된 숫자)
let 넘버 =a.match(/\d+/)[0].replace(/^0+/, "");
///\d+/
//\d : 숫자 
// + : 앞의 표현식이 1회 이상 

///^0+/ ??
// 앞의 0 을 지워주는 식 

 

 